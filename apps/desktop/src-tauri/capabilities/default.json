{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the DAIRR desktop window, including authenticated update operations.",
  "windows": ["main"],
  "permissions": ["core:default", "updater:default"]
}
