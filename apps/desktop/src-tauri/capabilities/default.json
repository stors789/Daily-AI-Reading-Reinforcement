{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimum permissions for the DAIRR desktop window. Updates are controlled by native Rust code.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
